# FRDDM-DQN 프로젝트 전체 파일 구조 설명

> **프로젝트 한 줄 요약:**
> 드론이 AI 카메라에 의존해 날아다니는데, 누군가 카메라를 속이면 드론이 어떻게 되는지 실험하고, 해결책을 만들어 검증하는 프로젝트.

---

## 전체 흐름

```
사진 만들기
  → AI 눈 학습
    → 공격 실험 (FGSM / PGD)
      → 영향 분석 (드론 항법 오차)
        → 해결책 설계 (CE 필터)
          → 드론 학습 (v2 → v3 → v4 → v5)
            → 성능 평가 (Confusion Matrix, 비교표)
              → 최종 정리 (final_summary.png)
```

---

## 1단계: 재료 준비

---

### `src/generate_images.py`

**이 파일이 하는 일:** 군용 차량이 있는 가짜 항공 사진 5장을 컴퓨터로 그려서 만든다.

**입력:** 없음 (아무것도 받지 않고 처음부터 그림)

**출력:** `data/images/` 폴��에 차량 그림 5장 (vehicle_01.png ~ vehicle_05.png)

**중요한 변수:**
- `IMG_W`, `IMG_H` = 1000, 500 → 사진 크기(가로 1000픽셀, 세로 500픽셀)를 결정
- `VEHICLE_COLORS` → 차량을 어떤 초록색으로 칠할지 결정 (군용 위장색 모음)

---

### `src/train_frcnn.py`

**이 파일이 하는 일:** AI가 사진에서 차량을 찾아 네모 박스(바운딩 박스)를 치는 방법을 학습시킨다.

**입력:** `data/images/` 폴더의 차량 사진, `data/gt_annotations.json` (정답 박스 위치)

**출력:** `results/frcnn_finetuned.pth` (학습된 AI 눈), `results/bbox_clean.json` (공격 없을 때 박스 결과)

**중요한 변수:**
- `NUM_EPOCHS = 3` → AI를 몇 번 반복해서 공부시킬지 (3번)
- `SCORE_THRESH = 0.3` → "이 정도 이상 확신할 때만 박스를 그려" (30% 이상)

---

## 2단계: 카메라 공격 실험

---

### `src/attack.py`

**이 파일이 하는 일:** 학습된 AI 카메라에 FGSM과 PGD 두 가지 방법으로 이미지를 살짝 바꿔서 박스가 얼마나 틀려지는지 측정한다.

**입력:** `results/frcnn_finetuned.pth` (학습된 AI 눈), 차량 사진 5장

**출력:** `results/attack_results.json` (FGSM 결과), `results/attack_results_pgd.json` (PGD 결과), 그래프 PNG 2개

**중요한 변수:**
- `EPSILONS = [0, 0.01, 0.03, 0.05, 0.07, 0.1]` → 공격 세기 목록 (0=공격없음, 0.1=가장 강한 공격)
- `PGD_STEPS = 10` → PGD 공격은 10번 반복해서 조금씩 이미지를 망가뜨림

---

### `src/detection_failure.py`

**이 파일이 하는 일:** 공격받은 사진에서 AI가 차량을 못 찾거나(Miss), 없는 차량을 봤다고 하는(Ghost) 비율을 계산한다.

**입력:** `results/attack_results.json` (FGSM), `results/attack_results_pgd.json` (PGD), `data/gt_annotations.json` (정답)

**출력:** `results/detection_failure.json` (실패율 숫자), `results/detection_failure.png` (그래프)

**중요한 변수:**
- `IOU_THRESH = 0.5` → 박스가 정답과 절반 이상 겹쳐야 "맞다"고 ��정 (50% 기준)
- `"miss_cnt"`, `"ghost_cnt"` → 각 사진·각 공격 세기별로 실제로 몇 번 실패했는지 기록

---

## 3단계: 오차가 드론에 미치는 영향 분석

---

### `src/analyze.py`

**이 파일이 하는 일:** 박스가 틀려진 픽셀 오차를 드론의 실제 거리·각도 오차로 환산해서 "공격이 드론 항법에 얼마나 위험한지" 수치로 보여준다.

**입력:** `results/attack_results.json` (FGSM 결과)

**출력:** `results/state_vector_error.png` (드론 상태 오차 그래프)

**중요한 변수:**
- `TAU = 0.05` → 픽셀을 실제 거리��� 바꾸는 스케일 계수 (논문 수식에서 가져온 값)
- `EPSILONS` → 공격 세기별로 오차가 얼마나 커지는지 비교

---

### `src/improved_ddm.py`

**이 파일이 하는 일:** "공격받은 경험은 학습에서 걸러내자"는 새로운 판단 규칙(개선된 DDM)을 코드로 설계한다.

**입력:** 없음 (다른 파일들이 이 파일을 도서관처럼 가져다 씀)

**출력:** 없음 (코드 부품 모음 — `DQNAgent`, `DDMState` 등 클래스 제공)

**중요한 변수:**
- `CE_THRESH_PX = 10.0` → 박스가 10픽셀 이상 밀렸으면 "오염된 경험"으로 판정
- `RISK_DIST` → 장애물에서 이 거리 안에 들어오면 위험 상태로 간주

---

### `src/summarize.py`

**이 파일이 하는 일:** FGSM 공격 결과를 4개 그래프로 한 장에 요약해 보여준다 (초기 버전 요약 그림).

**입력:** `results/attack_results.json`

**출력:** `results/final_summary.png` (초기 요약 그래프 — 나중에 덮어씌워짐)

**중요한 변수:**
- `TAU = 0.05` → analyze.py와 같은 스케일 계수
- 서브플롯 4개 → bbox 오차, 거리 오차, 각도 오차, 신뢰도 변화

---

## 4단계: 드론 학습 실험

---

### `src/dqn_comparison.py`

**이 파일이 하는 일:** 공격받는 환경에서 "기존 방식(모두 저장)"과 "개선 방식(오염 경험 차단)"을 처음으로 비교 학습한다.

**입력:** 없음 (환경 직접 생성)

**출력:** `results/dqn_comparison.png`, `results/dqn_results.json`, `DQNAgent`·`pgd_attack` 등 부품 제공

**중요한 변수:**
- `MAX_STEPS = 200` → 드론이 한 번 시도에서 최대 200걸음만 걸을 수 있음
- `CE_THRESH = 10.0` → 박스가 10픽셀 이상 틀렸으면 그 경험은 배우지 않음

---

### `src/visualize_flight.py`

**이 파일이 하는 일:** 기존 DDM과 개선 DDM 드론이 실제로 어떤 경로로 날아다녔는지 10개 시나리오를 그림으로 비교한다.

**입력:** 없음 (내부에서 드론을 직접 학습·실행)

**출력:** `results/flight_paths.png` (10행 × 2열 비행 경로 그림)

**중요한 변수:**
- `N_TEST = 10` → 10가지 다른 출발지·목적지로 테스트
- `OBS_RADIUS = 1.5` → 장애물 반경 1.5km 안에 들어오면 충돌 판정

---

### `src/dqn_comparison_v3.py`

**이 파일이 하는 일:** 장애물을 5개→8개로 늘리고 "연속 가중치 CE"(오염 정도에 따라 조금씩 배우는 방식)를 처음 실험한다.

**입력:** 없음

**출력:** `results/dqn_comparison_v3.png`, `results/dqn_results_v3.json`

**중요한 변수:**
- `deposit_ratio = max(0.05, 1.0 - shift/50)` → 박스가 많이 틀릴수록 이 경험에서 배울 확률이 낮아짐 (5%~100%)
- 장애물 고정 배치 `(1.0,1.0)→(13.5,16.5)` → 항상 같은 좁은 길을 통과해야 함

---

### `src/env_test.py`

**이 파일이 하는 일:** CE 필터 없이 "5장애물 환경"과 "8장애물 환경"만 따로 테스트해서 문제가 장애물 수 때문인지, 고정 경로 때문인지 원인을 분리한다.

**입력:** 없음

**출력:** `results/env_test.png`, `results/env_test_results.json`, `DroneEnvN`·`OBSTACLES_8` 등 부품 제공

**중요한 변수:**
- `OBSTACLES_8` → 8개 장애물 위치 목록 (다른 모든 v4·v5 실험이 이 목록을 그대로 씀)
- `SPACE_W, SPACE_H = 15, 18` → 드론이 날아다니�� 공간 크기 15km × 18km

---

### `src/dqn_comparison_v4.py`

**이 파일이 하는 일:** 8장애물 랜덤 환경에서 "이진 차단"(V4-A)과 "연속 가중치"(V4-B) CE 방식을 순수하게 비교한다.

**입력:** 없음

**출력:** `results/dqn_comparison_v4.png`, `results/dqn_results_v4.json`

**중요한 변수:**
- `CE_BINARY_THRESH = 10.0` → V4-A에서 10픽셀 넘으면 무조건 버림
- `CE_SHIFT_MAX = 50.0` → V4-B에서 50픽셀 이상이면 저장 확률이 최저(5%)로 고정

---

### `src/animate_flight.py`

**이 파일이 하는 일:** V4-A와 V4-B 드론이 5개 시나리오를 비행하는 모습을 GIF 애니메이션으로 만든다.

**입력:** 없음 (내부에서 V4 설정으로 드론 재학습)

**출력:** `results/flight_animation_v4.gif`

**중요한 변수:**
- `SCENARIO_SEED = 2025` → 어떤 5개 시나리오를 고를지 결정하는 씨앗 숫자 (같은 숫자면 항상 같은 시나리오)
- `DOT_SIZE_SCALE = 5.0` → 경로 위 주황색 오염 점 크기 (shift가 클수록 점이 ���짐)

---

### `src/dqn_comparison_v5.py`

**이 파일이 하는 일:** V4에 "시간 페널티"와 "거리 보상"을 추가해서 드론이 빙빙 돌지 않고 목적지로 직진하도록 개선한다.

**입력:** 없음

**출력:** `results/dqn_comparison_v5.png`, `results/dqn_results_v5.json`

**중요한 변수:**
- `TIME_PENALTY = -0.01` → 매 걸음마다 -0.01점 → 빠를수록 유리하게 만드는 채찍
- `DIST_APPROACH = +0.01` → 목적지에 가까워질 때 +0.01점 → 방향을 잘 잡으면 당근 지급

---

## 5단계: 성능 평가

---

### `src/ce_evaluation.py`

**이 파일이 하는 일:** "CE 필터가 진짜 나쁜 경험을 잘 걸러내는가?"를 숫자로 평가한다 — 임계값 전략 비교표와 순환논리 없는 Confusion Matrix를 만든다.

**입력:** `results/dqn_results_v4.json`, `results/dqn_results_v5.json`, `results/env_test_results.json`, `results/detection_failure.json`, `results/attack_results_pgd.json`

**출력:** `results/threshold_summary_table.png` (전략별 비교표), `results/ce_confusion_matrix_v2.png` (정직한 성능 평가)

**중요한 변수:**
- `threshold_px = 10.0` → "shift가 10픽셀 넘으면 CE"라는 우리 판별기의 기준선
- TP=25, FP=0, TN=2, FN=3 → 30개 데이터에서 나온 혼동행렬 결과 (직접 계산)

---

### `src/animate_flight_v5.py`

**이 파일이 하는 일:** V5-A와 V5-B 드론이 5개 시나리오를 비행하는 GIF를 만들고, V4의 "빙빙 돌기 시간초과" 문제가 해결됐는지 결과표로 보여준다.

**입력:** 없음 (내부에서 V5 설정으로 드론 재학습)

**출력:** `results/flight_animation_v5.gif`

**중요한 변수:**
- `TIME_PENALTY = -0.01`, `DIST_APPROACH = +0.01` → V5 보상 파라미터 (dqn_comparison_v5.py와 동일)
- `PAUSE_FRAMES = 25` → 각 시나리오 끝에서 25프레임 동안 결과 화면을 멈춰서 보여줌

---

### `src/final_summary.py`

**이 파일이 하는 일:** 프로젝트의 모든 핵심 결과를 6개 그래프로 한 장에 모아서 "최종 발표 슬라이드"처럼 정리한다.

**입력:** `attack_results.json`, `attack_results_pgd.json`, `detection_failure.json`, `dqn_results_v5.json` (기존 파일만, 새 실험 없음)

**출력:** `results/final_summary.png`

**중요한 변수:**
- `rolling_mean(..., window=50)` → 학습 곡선을 50에피소드 평균으로 부드럽게 표시
- 서브플롯 6개 → (1)공격비교 (2)탐지실패 (3)전략비교 (4)학습곡선 (5)CM (6)시나리오결과

---

## 결과 파일 목록

| 파일 | 만든 스크립트 | 설명 |
|------|--------------|------|
| `results/frcnn_finetuned.pth` | train_frcnn.py | 학습된 AI 카메라 모델 |
| `results/bbox_clean.json` | train_frcnn.py | 공격 없을 때 정상 박스 |
| `results/attack_results.json` | attack.py | FGSM 공격 결과 |
| `results/attack_results_pgd.json` | attack.py | PGD 공격 결과 |
| `results/detection_failure.json` | detection_failure.py | Miss/Ghost 실패율 |
| `results/env_test_results.json` | env_test.py | 5vs8 장애물 기준 도착률 |
| `results/dqn_results.json` | dqn_comparison.py | v2 실험 결과 |
| `results/dqn_results_v4.json` | dqn_comparison_v4.py | V4-A/B 실험 결과 |
| `results/dqn_results_v5.json` | dqn_comparison_v5.py | V5-A/B 실험 결과 |
| `results/threshold_summary_table.png` | ce_evaluation.py | 전략별 성능 비교표 |
| `results/ce_confusion_matrix_v2.png` | ce_evaluation.py | CE 판별기 Confusion Matrix |
| `results/flight_animation_v4.gif` | animate_flight.py | V4 비행 애니메이션 |
| `results/flight_animation_v5.gif` | animate_flight_v5.py | V5 비행 애니메이션 |
| `results/final_summary.png` | final_summary.py | 최종 6-패널 요약 그림 |
